<view class="container">
  <view class="post-comment">
    <view class="input-box">
      <textarea class="content" focus="true" bindinput="bindInpuntValue" maxlength="140" placeholder="评论经过筛选后，对所有人可见" />
      <text class="count">{{140 - content.length}}</text>
    </view>
    <!-- 图片上传 -->
    <!-- <view class="selectimg" bindtap="Selectimg" hidden="{{SelectHidden}}">+</view>
    <view class="previewImg">
      <view wx:for="{{preview_piclist}}" wx:for-item="itemName" class="item">
        <view class="delpic" id="{{index}}" bindtap="delpic">
          <label class="text">-</label>
          <view class="bg"></view>
        </view>
        <image src="{{itemName}}" class="img" mode="aspectFit"></image>
      </view>
      <view class="clear"></view>
    </view> -->

    <view class="btns">
      <view class="close" bindtap="onClose">取消</view>
      <view class="post" bindtap="onPost">发表</view>
    </view>
  </view>
</view>